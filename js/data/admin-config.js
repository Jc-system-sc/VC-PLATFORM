/* Credenciales del panel admin — cambia estos valores.
   No usa Firebase Auth: es una validación simple en el cliente. */

const ADMIN_CREDENCIALES = {
  usuario: "admin",
  clave: "vueloCreativo2026"
};
